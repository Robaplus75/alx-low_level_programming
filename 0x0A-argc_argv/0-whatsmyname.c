#include <stdio.h>
#include "main.h"
/**
 * main - prints its name
 * @argc: number of arguments
 * @argv: argument
 * Return: none
 */
int main(int argc, char **argv)
{
	printf("%s\n", argv[0]);

	return (0);
}
