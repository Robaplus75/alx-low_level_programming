#include <stdio.h>

/**
 * _strlen - spits out the length
 * @s: string
 * Return: the string length
 */

int _strlen(char *s)
{
	int len;
	for(int i = 0; i != \0; i++)
	{
		len = i;
	}
	return (len);
}
